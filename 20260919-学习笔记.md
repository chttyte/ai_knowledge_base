# 掌柜智库 学习笔记

> 用途：记录进度、踩过的坑、环境信息。**换会话 / 换电脑 / 隔几天回来时，先看这个。**
> 最后更新：2026-09-19

---

## 一、项目进度

### 导入链（7 个节点）

| # | 节点 | 业务文件 | 状态 |
|---|---|---|---|
| 1 | `node_entry` | `entry_service.py` | ✅ 完成（44 行） |
| 2 | `node_pdf_to_md` | `pdf_parse_service.py` | ✅ 完成（244 行） |
| 3 | `node_md_img` | `enrich_markdown_images.py` | ✅ 完成（233 行） |
| 4 | `node_document_split` | `split_service.py` | ⬜ 空壳（12 行）**← 下一个** |
| 5 | `node_item_name_recognition` | `item_name_service.py` | ⬜ 空壳（12 行） |
| 6 | `node_bge_embedding` | `embedding_service.py` | ⬜ 空壳（11 行） |
| 7 | `node_import_milvus` | `index_service.py` | ⬜ 空壳（12 行） |

- `main_graph.py` 已建，7 个节点已接线
- 节点文件都是脚手架（记账 + 转发），7 个都在
- **infra 层现状**：只有 `config` / `llm` / `object_storage`；缺 `persistence`(Mongo) / `vectorstore`(Milvus) / `document_parse`(MinerU)

### node_md_img 已验证的完整链路（2026-09-19）

```
✓ scan_images            返回 1 张（a.jpg），b.jpg 被过滤，日志有警告
✓ summarize_images       视觉模型真实调用，0.8 秒，摘要语义正确
✓ upload_images_and_replace  上传 310ms，URL 可访问（200 / image/jpeg / 48389 字节 = 原图大小）
✓ backup_markdown        生成 手册_new.md，原始 手册.md 保留
✓ enrich_markdown_images 总调度跑通，耗时 2137ms，退出码 0
```

**未验证**：`is_supported_image` 的过滤一（非图片文件被跳过）还没专门测。

---

## 二、踩过的坑（按症状查）

### A. 参数名类（最容易反复踩）

| 症状 | 病因 | 规律 |
|---|---|---|
| `Minio.remove_objects() got an unexpected keyword argument 'delete_objects'` | 参数名是 `delete_object_list` | **用 `Ctrl+P` 看签名，别猜参数名** |
| `KeyError: 'image_content'` | `load_prompt` 内部 `.format(**kwargs)`，参数名必须和 `.prompt` 里的占位符一致（`{image_content[0]}`） | **调 `**kwargs` 接口前，先打开模板文件看占位符叫什么** |
| `TypeError: 'Minio' object is not callable` | `client` 是 `@property`，不能加括号 | **`@property` → 不加括号；普通 `def` → 加括号** |
| `AttributeError: 'InfraConfig' object has no attribute 'minio_config'` | 字段名是 `minio`，不是 `minio_config` | **`infra_config.` 后面只跟短词**：`app/llm/embedding/reranker/mcp/milvus/mineru/minio` |

> **`.format()` 的行为要记住**：模板里有、你没给 → **KeyError**；你给了、模板里没有 → **静默忽略**。

### B. 导入类

| 症状 | 病因 |
|---|---|
| `ImportError: cannot import name 'fetch_chunks_by_chunk_ids' from 'app.shared.clients.milvus_utils'` | `clients/__init__.py` 里多写了这个名字。**父包 `__init__.py` 坏了会连累所有子模块**（导入子模块先执行父包） |
| `ModuleNotFoundError: No module named 'pyarrow.array'` | 编造的导入（`pyarrow.array` 是个函数不是模块） |
| `from sympy.codegen.ast import none` | PyCharm 自动导入选错，跟项目毫无关系 |
| `ModuleNotFoundError: No module named 'app'` | 命令行跑 `test/` 下的文件要加 `PYTHONPATH=.` |

> **规律：报错位置和改动位置不一致时，顺着 import 链往上找，看 traceback 的最后一条 `File "...", line N`。**

### C. 逻辑写反类

| 症状 | 病因 |
|---|---|
| 过滤完全失效，非图片文件也被处理 | `if is_supported_image(...)` 漏了 `not` **且** `is_supported_image` 没取后缀（`os.path.splitext(f)[1]`）。**两个错恰好互相抵消** |
| PDF 被路由到图片处理节点 | PDF 分支里 `is_md_read_enabled=True` / `is_pdf_read_enabled=False` 写反了 |

> **自检方法**：条件语句 + 它里面的日志/注释，**连起来读必须通顺**。日志说"不是图片"而条件是"是图片"，就是写反了。

### D. LangGraph 类

| 症状 | 病因 |
|---|---|
| `ValueError: Found edge ending at unknown node <function node_entry ...>` | `set_entry_point(node_entry)` 传了函数，**应该传字符串** `"node_entry"` |
| 状态里全是空值 | `StateGraph(ImportGraphState())` 传了实例（`{}`），**应该传类本身** |
| `KeyError: '__end__'` | 条件边映射表键写成 `"END"`，而路由函数 `return END`（值是 `"__end__"`）。**键必须等于路由返回值** |

### E. 环境 / 网络类

| 症状 | 病因 |
|---|---|
| 国内服务（mineru.net、cdn-mineru）连不上，TLS 握手被切断 | **Clash Verge 的 TUN 模式**劫持 DNS，把域名解析成 `198.18.x.x` 假 IP |
| 图片显示 "image load failed" | 原始 md 用**相对路径** `images/xxx.jpg`，打开时 md 和 images 目录不在一起 |
| 压缩包里双击打开 md，图片全挂 | 压缩包浏览器只把那**一个文件**解压到临时目录，`images/` 不在旁边 |

> **快速判断代理劫持**：
> ```python
> import socket
> print(socket.gethostbyname("随便一个域名"))
> ```
> **解析出 `198.18.x.x` → 就是代理在接管**，跟代码无关，去看代理软件。
>
> **修复**：给 `mineru.net`、`openxlab.org.cn` 加直连规则（它们是国内服务）。

### F. 文件 / 路径类

| 症状 | 病因 |
|---|---|
| 产物落到了 `app/process/.../nodes/output/`，不是项目根的 `output/` | **相对路径 `Path("output/xxx")` 以"工作目录"为基准**，PyCharm 默认工作目录 = 脚本所在目录 |
| 三引号里的测试数据每行多了 4 个空格 | 三引号字符串跟着代码缩进了，**内容要顶格写** |

> **规律："工作目录"、"脚本位置"（`__file__`）、"项目根"是三件不同的事。**
> **代码里一律用 `PROJECT_ROOT / "..."`**（`PROJECT_ROOT` 从 `app.shared.runtime.logger` 导入）。

---

## 三、环境与配置

| 项 | 值 |
|---|---|
| **MinIO** | `39.105.7.90:9000`（**共享服务器**，桶 `knowledge-base-files`，公开读，前缀 `/upload-images`） |
| **视觉模型** | `qwen3-vl-flash` @ `dashscope.aliyuncs.com/compatible-mode/v1` |
| **文本模型** | `qwen-flash` |
| **MinerU** | `https://mineru.net/api/v4` |
| **MongoDB** | 库名 `kb002` |
| **代理** | Clash Verge（端口 7897），**TUN 模式下国内服务要加直连规则** |

**⚠️ 别在共享 MinIO 上删别人的数据**：`upload_images_and_replace` 第一步会按 `upload-images/{stem}/` 前缀删旧图。**用讲师的文档（`hak180产品安全手册`）跑会删掉他上传的图片。**

---

## 四、调试方法

### 单个函数调（推荐）

在文件底部加 `if __name__ == '__main__':` 块，**一次只调一个函数**：

```python
if __name__ == "__main__":
    from pathlib import Path
    from app.shared.runtime.logger import PROJECT_ROOT

    d = PROJECT_ROOT / "output" / "迷你测试"        # 用 PROJECT_ROOT，不用相对路径
    (d / "images").mkdir(parents=True, exist_ok=True)
    (d / "手册.md").write_text(
"""# 测试文档

装纸前请注意：

![旧说明](images/a.jpg)
""", encoding="utf-8")            # ← 内容顶格写，别跟着缩进
    (d / "images" / "a.jpg").write_bytes(真图片字节)

    result = scan_images((d / "手册.md").read_text(encoding="utf-8"), d / "images")
    print("返回了几张:", len(result))
```

**为什么比"跑整个流程"强**：出错时立刻知道是哪个函数，准备数据只要秒级，只依赖这一步要的东西。

### 造测试数据的原则

| 原则 | 说明 |
|---|---|
| **图片要真** | `b"\xff\xd8\xff\xe0fake"` 只是起了个 JPEG 头，模型解析会失败 |
| **要有"标准答案"** | 用有明确内容的图（比如"高温警示"那张），才能判断模型答得对不对 |
| **故意设计边界** | 同一张图引用两次（测全局替换）、放一张没引用的图（测过滤二）、放一个 `.txt`（测过滤一） |

### 验证要走到最后一步

**"代码跑通、日志没报错" ≠ "结果是对的"。**

比如上传图片：上传成功、URL 拼得出来，但**如果桶没设公开读，访问时是 403**。这种"上游全对、下游用不了"的问题，**只有真的请求一次 URL 才会暴露**。

> **凡是"生成一个地址给别人用"的功能，都自己先访问一次。**

### 常用快捷键

| 快捷键 | 作用 |
|---|---|
| `Ctrl + P` | 参数签名（括号里最常用） |
| `Alt + Enter` | 自动导入 / 快速修复 |
| `Ctrl + R` | 当前文件替换 |
| `Ctrl + B` | 跳到定义 |
| `Ctrl + Alt + O` | 清理多余 import（**只删不加**） |
| `Ctrl + Shift + F10` | 运行当前文件 |

### 怎么区分两种红线

| 提示文字 | 性质 | 菜单里有"导入"吗 |
|---|---|---|
| "未解析的引用 'Dict'" | 导包问题 | **有** → 选「导入此名称」 |
| "Typo" / 拼写错误 | 拼错了 | **没有** → 改名字 |

**注意**：菜单里的「**安装并导入软件包**」通常是错的方向——你要的大多是"已有模块里的一个名字"，不是"装一个新包"。

### 自动导入的边界

| 来源 | 自动导入 | 例子 |
|---|---|---|
| 标准库 | ✓ 安全 | `typing`、`pathlib`、`re`、`os` |
| 第三方包 | ✓ 安全 | `requests`、`loguru`、`minio` |
| **项目自己的模块** | ✗ **手动写** | `app.shared.runtime.logger`（`logger` 会被误导成 loguru 的原版） |

---

## 五、学习偏好（给自己和 AI 的提示）

- **回答要短**，术语要顺带解释
- **要具体例子**，不要抽象原则
- **先跑通、后理解**，别一上来讲一堆概念
- 遇到"两个词的区别"这类问题，直接给**能跑的最小例子**

---

## 六、下一步

**`node_document_split`（文档切分）**

- 业务文件：`app/rag/import_/split_service.py`（12 行空壳）
- 教案：`教案/05【掌柜智库】【导入】【节点4】node_document_split.md`
- 参考实现：`代码/ai_0119_rag/app/rag/import_/split_service.py`
- 用到的常量（要加进 `app/rag/import_/config.py`）：
  ```python
  CHUNK_MAX_SIZE = 1000
  CHUNK_SIZE = 600
  CHUNK_OVERLAP = 20
  ```
- 读 `md_content`，写 `chunks`；`file_title` 用来给每个 chunk 补 metadata

**参考实现里的 7 个函数（路线图）**：

```
load_markdown_content(state)                     读 md 内容
split_by_titles(md_content, file_title)          ★ 按标题切成 section
_split_long_section(section, max_length=...)     太长的 section 再切
_merge_short_sections(...)                       太短的合并
refine_chunks(...)                               整理
backup_chunks(chunks, md_path)                   备份 chunks.json
split_document(state)                            总调度
```

**⚠️ 提前预警：`split_by_titles` 依赖"标题"，但 MinerU 输出的标题有两个问题**

之前验证节点 3 时实测过：

1. **所有标题都是一级 `#`，没有 `##`/`###`** —— 层级信息丢失（实测那个文件：`#` 19 个，`##` 0 个）
2. **正文被误判成标题** —— 比如 `# 对于本设备所有者不遵守本指南中规定的说明操作而导致的损害，Brother 不承担任何责任。` 这明明是免责声明正文

**所以"按标题切"在真实数据上会发生什么，是下一个节点要重点观察的。** 可能的结果：

- 还行（只要标题存在就能当边界，不管层级）
- 或者切得太碎（被误判成标题的正文各自成了一个小 section）

**这是 PDF 解析的固有误差，不是代码的错。** 到时候多看几份真实文档的输出，判断影响有多大。

**教案里可能有的错**（前面几节都出现过写漏/写反的情况，抄的时候对着参考实现核一遍）
