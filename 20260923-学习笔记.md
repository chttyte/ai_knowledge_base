# 掌柜智库 学习笔记（二）

> 接 `20260919-学习笔记.md`，那篇记的是导入链前 3 个节点。
> 这篇记 2026-09-23 这一轮：导入链**全流程跑通** + 一堆 Milvus / 编码 / 静默失败的坑。
> 用途不变：**换会话 / 换电脑 / 隔几天回来时，先看这个。**

---

## 一、项目进度

### 导入链（7 个节点）—— ✅ 全部跑通

```
✅ node_entry              ✅ node_pdf_to_md        ✅ node_md_img
✅ node_document_split     ✅ node_item_name_recognition
✅ node_bge_embedding      ✅ node_import_milvus
```

**2026-09-23 实测 `main_graph.py` 全流程跑通**（`doc/hak180产品安全手册.pdf`）：

```
📄 PDF转MD ✓   🏷️ 主体名称：HAK 180 烫金机
📝 切片 9 个   🔍 全部向量化：是      ⚠️ 含 chunk_id：否  ← 待补
```

Milvus 里实际查到 9 行，`kb_chunks` 建好了（两个索引都在）。

### 还没做的

| 项 | 说明 |
|---|---|
| **`chunk_id` 回写** | `index_chunks` 的 docstring 承诺了，代码里没做（insert 返回的 ids 没写回 chunks） |
| **检索链 7 个节点** | 完全没开始 |
| **Web 服务集成** | 在学 FastAPI（教案 06） |
| **SSE 三处补齐** | 工具层有了，缺「建队列 + SSE 路由 + 前端 EventSource」 |

---

## 二、踩过的坑（按症状查）

### A. 静默失败类 ★ 这一轮出现了 8 次以上，最该记住的一类

| 症状 | 病因 |
|---|---|
| `load_collection` 传了 schema/index_params，却什么都没建 | `load_collection(collection_name, timeout=None, **kwargs)` —— **`**kwargs` 把参数吞了**，不报错不警告。建集合要用 `create_collection` |
| 集合永远建不出来，日志一切正常 | `has_collection → return` 守卫取错集合名 → 检查的是另一个集合 → 永远命中 → 直接返回 |
| 坏集合永远修不好 | 同上。**「幂等保护」变成「错误固化」**：对象已经存在，代码每次跳过，再也不会重建它 |
| filter 写不存在的字段名，返回 0 行 | Milvus **不报错**，静默返回空 |
| 下载失败后，把错误响应当 zip 写进文件 | `if status_code != 200: logger.error(...)` **只 log 不 raise**，继续往下走 |
| 重试用完之后用未定义的变量 | `while retry_time:` 退循环后没检查，`response` 可能从未被赋值 → `NameError` |
| `get_collection_stats` 返回 4，实际 9 行 | 那是**近似值**（growing segment 没合并）。判断插没插进去要用 `query` + `consistency_level="Strong"` |

> **一句话规律**：**「我给了东西但没生效」→ 去找「吞掉」或「静默跳过」的地方。**
> 常见吞法：`**kwargs`、`.format()`、`if not x` 当有效性检查、只 log 不 raise、`except` 后 `continue`。

### B. Milvus 类

| 症状 | 病因 |
|---|---|
| `no vector index on field: [sparse_vector]` | 索引**从来没建成**。根因是上面那个 `load_collection` |
| `dimension is not defined in field type params` | `FLOAT_VECTOR` 要用 `dim=`，**不是 `max_length=`**（那是 VARCHAR 的参数） |
| `metric type not match: expected=COSINE actual=IP` | **建索引的 metric 必须和检索侧一致**。BGE-M3 向量做了 L2 归一化，IP 和 COSINE 排序结果相同，选哪个都行但不能一边一个 |
| `Insert missed an field 'file_title'` | 插入的 key 写成了 `file_name`。**动态字段开着** → 未知 key 被静默收下 → 于是报「缺字段」而不是「多字段」 |
| `float16 vector field is illegal ... actual=got nil` | 其实是**类型不对**（传了普通 list，要 np.float16）。别信 `got nil` 字面意思 |
| `SPARESE_INVERTED_INDEX` → `invalid parameter` | 拼写错误。**索引类型名是字符串，IDE 不给波浪线** |
| filter `item_name == 'Brother's 手'` 解析失败 | 单引号没转义。而且现有的 `escape_milvus_string` **只转义双引号**，挡不住单引号 |
| 报错里的集合是个数字（如 `468507609007489882`） | 那是 Milvus 内部 ID，不是你的 bug。用 `describe_collection` 的 `collection_id` 对一下就知道是哪个集合 |
| `collection not loaded` | 集合没 load 进内存。`create_collection` **自带 load**（源码里 `create_index` 后紧跟 `load_collection`），所以报错的集合≠你刚建的 |

> **Milvus 报错会指向错误方向**：`got nil` = 类型不对、`缺字段` = 名字写错、`invalid parameter` = 拼写错。
> **值明显不是 nil 却报 got nil、字段明显给了却说缺字段 → 先回头查拼写和类型。**

### C. 编码 / 环境类

| 症状 | 病因 |
|---|---|
| **同一份代码，PyCharm 崩、命令行不崩** | `read_text()` 不指定编码 → 用系统默认。**PyCharm 继承 Windows 的 GBK，Git Bash 有 `PYTHONUTF8=1` 是 UTF-8** |
| `SSL: UNEXPECTED_EOF_WHILE_READING` | 代理劫持（Clash TUN）。判断：`socket.gethostbyname("任意域名")` 解析出 `198.18.x.x` 就是它在接管 |
| `Invalid HTTP request received` | curl 把中文直接塞进 URL。要用 `-G --data-urlencode` |
| `Errno 10048 端口被占用` | 上次的服务没关干净。关掉那个窗口，别改代码 |

> **`read_text()` / `write_text()` / `open()` 一律显式写 `encoding="utf-8"`。**
> 这类坑很隐蔽：**要文件里恰好出现那个字节才炸**，换个文档可能就"正常"，下次换回来再炸。

### D. 逻辑写反 / 写漏类

| 症状 | 病因 |
|---|---|
| `TypeError: 'type' object does not support item assignment` | `chunks = list[dict] = []` —— **冒号（类型标注）写成了等号**（链式赋值） |
| `AttributeError: 'str' object has no attribute 'read_text'` | `md_path` 是 str。同一个函数里另一处用了 `Path(md_path).stem`，这处没用 |
| **下载成功后无限重复下载** | `while retry_time:` 里**成功那条路没有 `break`**，`retry_time` 只在 except 里减 |
| `if not response` 判断"请求失败" | `requests.get` **失败时是抛异常，不返回假值**。而且 `bool(Response)` 是 `status_code < 400`，所以 `not response` 其实是"4xx/5xx" |
| 一次网络抖动打死整个节点 | 下载请求**没包 try**（轮询那个包了） |
| 测试跑一次就多一条垃圾数据 | 测试数据里两条 chunk 的 `item_name` 不一致，而删除只按 state 里那一个值删 |
| 一次 task 的切片被别的产品覆盖 | `remove_old_chunks` **只按 item_name 过滤，不看 file_title** → 同一产品的第二份文档导入时，会把第一份的切片删掉 |

### E. FastAPI 类（这次新学的）

| 知识点 | 要点 |
|---|---|
| `response_model` | 管**出参**。多返回的字段**静默丢弃**；少必填字段/类型不对 → **500**（前端只看到 Internal Server Error，原因在服务端日志） |
| `-> Type` vs `response_model` | 平时 `->` Python **完全不执行**（只记在 `__annotations__`）；但在 FastAPI 里它**会被当成 response_model 用**。两个都写时以 `response_model` 为准 |
| 首页 | FastAPI **不自动给首页**。不声明 `/` 就是 404 |
| `BackgroundTasks` | 响应**先返回**，任务在之后跑。实测：响应 67ms，后台任务 1.8s |
| SSE | 报文格式 `event: xxx\ndata: {...}\n\n`（**末尾空行不能少**）；路由要 `media_type="text/event-stream"` |
| async 里调同步阻塞 | `queue.get()` 直接调会**卡死整个事件循环**，必须 `run_in_executor` |
| LangGraph `stream_mode` | `"values"` 返回整个 state（`list(step.keys())[-1]` 拿到的是**字段名**）；`"updates"` 才返回 `{节点名: 输出}`。**不指定时默认就是 updates** |

> ⚠️ 教案 5.8 的测试代码用了 `stream_mode="values"` 却想拿节点名 —— 它自己的「示例日志」都写着节点名，**那段日志是编的**。教案 6.2 的 web 服务代码是对的（不指定 mode）。

---

## 三、环境与配置

| 项 | 值 |
|---|---|
| **Milvus** | 已换成 **Zilliz Cloud serverless**（阿里杭州）<br>`https://in03-be1ee336e2f7047.serverless.ali-cn-hangzhou.cloud.zilliz.com.cn` |
| **集合** | `kb_item_names`（已建：sparse 类型 104 + 两个索引 + Loaded）<br>`kb_chunks`（已建：9 行测试数据） |
| **BGE-M3** | 本地 CPU，`D:\ai_models\modelscope_cache\models\BAAI\bge-m3`，词表 250002 |
| **编码** | **Git Bash 有 `PYTHONUTF8=1`（utf-8），PyCharm 继承系统 GBK** ← 环境差异的根源 |
| **代理** | Clash Verge TUN 模式下 DNS 全被劫持成 `198.18.x.x`。用 mineru 时记得关，或加直连规则 |

**⚠️ `kb_item_names` 里有一条遗留数据**：`{'item_name': '华为Mate60 Pro手机', ...}`，不是导入链产生的，检索测试时可能干扰。

---

## 四、调试方法（这一轮新攒的）

| 方法 | 什么时候用 |
|---|---|
| **去日志文件捞完整堆栈** | `logs/app_YYYYMMDD.log` 里 `logger.exception` 会打完整堆栈。**截图只截半截时，去这里捞** |
| **grep 同类操作摆一起看** | `grep -n "_collection" app/rag/import_/index_service.py` → 三个函数的集合名一列，写错的一眼现形 |
| **建临时集合实测** | 别读代码猜。建个 `__tmp_xxx__` 的集合，插假数据、跑坏地址，用完 drop |
| **对照参考实现 diff** | `掌柜智库项目/代码/ai_0119_rag/` 是讲师版，教案有歧义就看它 |
| **加 `consistency_level="Strong"`** | 插入后立刻查不到时，先排除"读得太快"，别怀疑插入失败 |
| **模拟目标环境复现** | `PYTHONUTF8=0 LANG=C python ...` 复现 GBK 环境的编码问题 |
| **`describe_collection` 看字段名** | 写 filter 之前先确认字段真名（`kb_chunks` 的主键叫 `chunk_id` 不叫 `pk`） |

---

## 五、该掌握到什么程度（这一轮想清楚的）

**分三层，第三层最重要——它能把清单闭合，止住"什么都要会"的焦虑。**

### 第一层：必须闭卷说得出来的（就这 10 条）

1. 链路有哪几个节点，每步的输入/输出是 state 里哪个字段
2. 为什么切片定在 1000 字左右
3. 为什么要稠密 + 稀疏两路（语义相近 vs 字面精确）
4. 为什么召回之后还要 rerank（召回快但不准，精排是另一件事）
5. 为什么切片和主体名分两个集合（向量库没有 JOIN，item_name 要冗余到每行）
6. 为什么幂等要「先删后插」，删除依据是什么
7. 为什么导入要后台任务 + 轮询（全流程 30 秒，同步会超时）
8. 为什么索引 metric 必须和检索侧一致
9. 为什么代码块和表格不能被切断
10. 自己踩过的静默失败形状（这条别人答不上来）

### 第二层：知道「有这回事 + 去哪查」

```
FastAPI    → 路由声明 / 接请求体 / 文件上传 / 后台任务 / SSE
pymilvus   → create_collection / create_index / load / insert / query / hybrid_search
MinerU     → 上传 → 轮询 → 下载 zip
LangGraph  → 加节点 / 连边 / stream 的两种 mode
BGE-M3     → 一次编码出 dense + sparse 两路
```

### 第三层：明确「不用管」

```
FastAPI 的参数细节、依赖注入、CORS 各参数
pymilvus 各索引类型的参数怎么调（M=64 还是 128）
MinerU 的接口字段
各种 Response 类的区别
前端 HTML/JS
```

> **判断标准：AI 能替你补上的，全是第三层。**
> **判断力靠参照物**——你脑子里得有「这段代码应该长什么样」，才能看出 AI 写的哪里不对。
> 所以默写留着，但默写**判断点和骨架**，别默写**字面代码**。

---

## 六、下一步

### 马上要做的

1. **`chunk_id` 回写** —— `insert` 返回的 `ids` 顺序和传入的 `chunks` 一致，`zip` 回去就行
2. **教案 06 Web 服务集成** —— 参考 `test/fastapi_demo.py`（我做的教学 demo，能跑）

### 要修的小问题

| 问题 | 位置 |
|---|---|
| filter 单引号没转义 | `index_service.py:94` `f"item_name=='{item_name}'"` |
| 一产品的多份文档会互相覆盖 | `remove_old_chunks` 只按 item_name 过滤，要不要加 `file_title` 得想清楚 |
| 一堆无关的自动导入（`torchvision` 等） | `pdf_parse_service.py` 开头，`Ctrl+Alt+O` 清一下 |
| `entity_name_collection` 在 `.env` 里没有 | 现在是空字符串，用到时会拿空名字查 Milvus |

### 待决策的风险

**`remove_old_chunks` 只按 item_name 删** → 你的 `doc/` 里有 `hak180产品安全手册.pdf` 和 `hak180使用说明书.pdf` 是同一个产品。如果两次导入识别出同一个 item_name，**导入第二份时会把第一份的切片全删掉**。

- 如果「一个产品只保留最新导入」是预期 → 不用动
- 如果要两份共存 → filter 要加 `file_title`

---

## 七、学习偏好（不变）

- **回答要短**，术语顺带解释
- **要具体例子**，不要抽象原则
- **先跑通、后理解**
- 遇到「两个词的区别」这类问题，直接给**能跑的最小例子**

**这一轮新加的**：
- **别"复习"，去"重建"** —— 关掉代码从空文件默写骨架，写不出来的地方才是真缺的
- **笔记只记「判断句」，不记操作步骤** —— API 名字查一下就有，记「存在这么一类坑」才值钱
- **默写的对象是「判断点和因果」，不是「字面代码」**
